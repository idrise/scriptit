# ScriptIt

```
import _ from 'lodash' // ^4

export const configuration = {
  name: "
}

export async function script() {
  
  await bash`
    ls dumper* > filelist.txt
  `
  await bash.terminal`
    npm init
  `
}
  
```
